import styled from 'styled-components'
import {Heading} from '../typography'

export const LogoWrapper = styled.div`

`

export const LogoStyled = styled.img`

`

export const LogoSloganStyled = styled(Heading)`

`